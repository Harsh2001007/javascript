function addNum(num1, num2) {
  let result = num1 + num2;
  console.log(result);
}

// addNum(5, 8);
// addNum;
// addNum();
// addNum(5, "8");

function greeting(name) {
  return `hello ${name}, welcome to the game`;
}

greeting("harsh");
// console.log(greeting("harsh"));

function calling(number) {
  console.log(`the number is ${number}`);
  return `i want to call to number ${number}`;
  console.log(`the number is ${number}`);
}

calling("8505813979");
console.log(calling("8700931554"));
